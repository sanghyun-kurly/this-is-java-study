package chapter7.section6.package2;

public class C {
    public void method() {
        // protected 접근제한자는 다른 패키지에서 생성자, 필드, 메소드에 접근할 수 없다.
        // A a  = new A();
        // a.field = "value";
        // a.method();
    }
}
